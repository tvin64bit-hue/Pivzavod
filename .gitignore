node_modules/
dist/
.astro/
.wrangler/
.env
.env.*
!.env.example
# Локальные секреты для `wrangler pages dev` — там лежит токен бота
.dev.vars
.dev.vars.*
.DS_Store
npm-debug.log*
# Сырой HTML с донора намеренно хранится в репозитории: он позволяет
# переразобрать статьи, не обходя донор повторно (Crawl-delay: 5 с/стр.).
