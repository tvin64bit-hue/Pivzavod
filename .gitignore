node_modules/
dist/
.astro/
# Каталог, который собирает scripts/prepare-content.mjs: архив из репозитория
# плюс записи, созданные через админку. Источники в git, результат — нет.
.content/
# Записи и фотографии, созданные через админку (на сервере /srv/pivzavod74/data)
data/
# Локальные настройки: там пароль от почтового ящика и хеш пароля админки
.env
.env.*
!.env.example
# Промежуточные сборки локального запуска
.releases/
deploy/nginx/*.local.conf
.DS_Store
npm-debug.log*
# Сырой HTML с донора намеренно хранится в репозитории: он позволяет
# переразобрать статьи, не обходя донор повторно (Crawl-delay: 5 с/стр.).
# Оригиналы до пересжатия — локальная страховка на случай отката.
# В репозиторий не тащим: при нужде перекачиваются с донора скриптом миграции.
assets/source-images/legacy-originals/
